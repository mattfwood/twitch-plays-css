{
  "name": "TwitchPlaysX",
  "private": true,
  "description": "Lets you use IRC to send inputs to a program and stream it, TwitchPlaysPokemon style",
  "version": "0.0.1",
  "dependencies": {
    "css": "^3.0.0",
    "sendkeys-macos": "^1.3.0",
    "tmi.js": "1.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "lint": "prettier --write *.js",
    "watch": "npx browser-sync start --server --files \"*.css\" -w"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hzoo/TwitchPlaysX.git"
  },
  "devDependencies": {
    "prettier": "2.0.5"
  }
}
